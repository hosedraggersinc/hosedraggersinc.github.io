const isProd = process.env.NODE_ENV === "production";

const repo = "hosedraggersinc.github.io";

export default {
  output: "export",
  basePath: "/hosedraggersinc.github.io",
  assetPrefix: "/hosedraggersinc.github.io",
  // other settings
};
